# LongevityCompetition SDK

Longevity Competition API client, generated from the OpenAPI spec.

The Longevity Competition API provides access to real-time information about participants, rankings, events, and historical data related to the Longevity World Cup.

Learn more about Longevity Competition API at [longevityworldcup.com/swagger](https://longevityworldcup.com/swagger).

This is an unofficial SDK for the Longevity Competition public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Metadata kindly supplied by [www.freepublicapis.com](https://www.freepublicapis.com).

> TypeScript, Python, PHP, Golang, Ruby, Lua SDKs, a CLI, an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as **7 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `create`):

```ts
const client = new LongevityCompetitionSDK()
const items = await client.Athlete().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
const client = LongevityCompetitionSDK.test()
const athletes = await client.Athlete().list()
// athletes is an array of bare Athlete records populated with mock data
console.log(athletes)
```

### Python

```python
client = LongevityCompetitionSDK.test()
athletes = client.Athlete().list()
print(athletes)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = LongevityCompetitionSDK::test([
    "entity" => ["athlete" => ["test01" => []]],
]);
$athletes = $client->Athlete()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Athlete(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = LongevityCompetitionSDK.test({
  "entity" => { "athlete" => { "test01" => {} } },
})
athletes = client.Athlete.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Athlete():list()
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/longevity-competition` | publish pending — [install from git tag](https://github.com/voxgig-sdk/longevity-competition-sdk/releases) |
| Python | `voxgig-sdk-longevity-competition` | publish pending — [install from git tag](https://github.com/voxgig-sdk/longevity-competition-sdk/releases) |
| PHP | `voxgig-sdk/longevity-competition` | publish pending — [install from git tag](https://github.com/voxgig-sdk/longevity-competition-sdk/releases) |
| Golang | `github.com/voxgig-sdk/longevity-competition-sdk/go` | `go get github.com/voxgig-sdk/longevity-competition-sdk/go@latest` |
| Ruby | `voxgig-sdk-longevity-competition` | publish pending — [install from git tag](https://github.com/voxgig-sdk/longevity-competition-sdk/releases) |
| Lua | `voxgig-sdk-longevity-competition` | publish pending — [install from git tag](https://github.com/voxgig-sdk/longevity-competition-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/longevity-competition-sdk/go-cli` | `go install github.com/voxgig-sdk/longevity-competition-sdk/go-cli/cmd/longevity-competition@latest` |
| Go MCP server | `github.com/voxgig-sdk/longevity-competition-sdk/go-mcp` | `go get github.com/voxgig-sdk/longevity-competition-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { LongevityCompetitionSDK } from '@voxgig-sdk/longevity-competition'

const client = new LongevityCompetitionSDK()

// List all athletes (returns Athlete[])
const athletes = await client.Athlete().list()
for (const athlete of athletes) {
  console.log(athlete)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua) | `ts/` `py/` `php/` `go/` `rb/` `lua/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o longevity-competition-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "longevity-competition": {
      "command": "/abs/path/to/longevity-competition-mcp"
    }
  }
}
```

## Entities

The API exposes 7 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Athlete** | The Athlete entity (list). | `/data/athletes` |
| **BortzAge** | The BortzAge entity (create). | `/data/bortz-age` |
| **Competition** | The Competition entity (list). | `/data/divisions` |
| **Leaderboard** | The Leaderboard entity (list). | `/data/leaderboard` |
| **PhenoAge** | The PhenoAge entity (create). | `/data/pheno-age` |
| **RankPreview** | The RankPreview entity (create). | `/data/rank-preview` |
| **Reference** | The Reference entity (list). | `/data/flags` |

The operations available across these entities are **list**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
from longevitycompetition_sdk import LongevityCompetitionSDK

client = LongevityCompetitionSDK()

# List all athletes (returns a list, raises on error)
athletes = client.Athlete().list()
for athlete in athletes:
    print(athlete)
```

### PHP

```php
<?php
require_once 'longevitycompetition_sdk.php';

$client = new LongevityCompetitionSDK();

// List all athletes (returns an array; throws on error)
$athletes = $client->Athlete()->list();
print_r($athletes);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/longevity-competition-sdk/go"

client := sdk.New()

// List all athletes
athletes, err := client.Athlete(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(athletes)
```

### Ruby

```ruby
require_relative "LongevityCompetition_sdk"

client = LongevityCompetitionSDK.new

# List all athletes (returns an Array; raises on error)
athletes = client.Athlete.list
puts athletes
```

### Lua

```lua
local sdk = require("longevity-competition_sdk")

local client = sdk.new()

-- List all athletes
local athletes, err = client:Athlete():list()
print(athletes)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

- Upstream API: [https://longevityworldcup.com](https://longevityworldcup.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Longevity Competition API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
